package quadraticEquatation;

public class Discriminant {

	public double discriminantCalc (double a, double b, double c) {//throws InterruptedException{
		
	//Thread.sleep(1);	
		
	double D = Math.pow(b, 2) - 4*a*c;
	return D;}
}